name: kanji_drawing_animation
description: A widget that draws Kanji with the right stroke order based on KanjiVG.
version: 0.5.0
repository: https://github.com/atn832/kanji_drawing_animation
homepage: https://www.wafrat.com

environment:
  sdk: '>=2.18.6 <3.0.0'
  flutter: ">=2.5.0"

dependencies:
  flutter:
    sdk: flutter
  svg_drawing_animation: ^0.10.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
